template: graph-lines-02.j2
datasource: influxdb
title: "lwaftr out-ipv6-bytes (delta)"
span: 6

targets:
  A:
    query: >
      SELECT mean(delta)
      FROM /($host_regex).lwaftr.statistics.($br_instance).out-ipv6-bytes/
      WHERE $timeFilter
      GROUP BY time($GroupBy)
templatings_used:
  - host_regex.yaml
  - br_instance.yaml
  - group_by.yaml
