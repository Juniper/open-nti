template: graph-lines-02.j2
datasource: influxdb
title: "lwaftr in-ipv6-packets (delta)"
span: 6

targets:
  A:
    query: >
      SELECT mean(delta)
      FROM /($host_regex).lwaftr.statistics..*.in-ipv6-packets/
      WHERE $timeFilter
      GROUP BY time($GroupBy)
templatings_used:
  - host_regex.yaml
  - group_by.yaml
