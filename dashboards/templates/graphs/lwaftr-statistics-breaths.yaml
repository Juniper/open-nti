template: graph-lines-02.j2
datasource: influxdb
title: "lwaftr breaths (delta)"
span: 6

targets:
  A:
    query: >
      SELECT mean(delta)
      FROM /($host_regex).lwaftr.statistics..*.breaths/
      WHERE $timeFilter
      GROUP BY time($GroupBy)
templatings_used:
  - host_regex.yaml
  - group_by.yaml
